function spinWheel() {
    // Xử lý quay vòng ở đây
    // Lấy giá trị email từ input
    var email = $('#email').val();

    // Gửi email và nhận phần quà từ server (cần phải triển khai một server để xử lý)

    // Thêm logic xử lý quay vòng và hiển thị kết quả
    // ...

    // Lưu kết quả vào file Excel (cần phải triển khai một phía server để thực hiện)
    // ...

    // Chạy nhạc nền
    document.getElementById('backgroundMusic').play();
}
